function [ output_args ] = joinclusters( clusters, size_thresh )
%UNTITLED Summary of this function goes here
%   Detailed explanation goes here
    clusters = sortrows(clusters,-6);
    [row col] = size(clusters);
    for i = 1:row
        if(clusters(i,6) < size_thresh)
            continue
        end
        for j = i+1:row
           if(clusters(i,6) 
        end
    end

end

